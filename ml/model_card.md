Model Card: mobile_money_fraud_v1

Summary:
- Model: RandomForestClassifier exported as joblib and optionally ONNX
- Trained on synthetic dataset generated by `generate_synthetic_transactions.py` (synthetic data only)

Metrics:
- Precision: see training output
- Recall: see training output
- F1: see training output
- AUC: see training output

Limitations:
- Trained on synthetic data — performance on real traffic is unknown.
- Must be retrained on labeled production data before being used for blocking decisions.

Retraining:
- Use `ml/train_model.py` to retrain and export new versions. Register the new model using `ModelRegistry.load_joblib` or `load_onnx`.
